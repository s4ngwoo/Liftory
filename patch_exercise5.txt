sed -i '/override fun observeAll/i \    override fun search(query: String): Flow<List<Exercise>> {\n        return exerciseDao.searchExercises("%$query%").map { list -> list.map { it.toDomain() } }\n    }\n\n    override fun getExercisesByCategory(category: String): Flow<List<Exercise>> {\n        return exerciseDao.getExercisesByCategory(category).map { list -> list.map { it.toDomain() } }\n    }\n' app/src/main/java/com/example/infrastructure/repository/ExerciseRepositoryImpl.kt
